/** @type {import('tailwindcss').Config} */
module.exports = {
  // ... other config
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-geist-sans)'],
        accent: ['var(--font-space-grotesk)'],
      },
    },
  },
  // ... other config
} 